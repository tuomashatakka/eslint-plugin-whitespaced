/**
 * @fileoverview Mocha test setup file
 * @author tuomashatakka
 */

// This allows us to use ES modules in tests without issues
export const mochaHooks = {
  beforeAll() {
    // Any global setup if needed
  }
};
